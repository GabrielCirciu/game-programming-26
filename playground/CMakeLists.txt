file(GLOB file_src_list CONFIGURE_DEPENDS "*.c" "*.cpp")

foreach(file_src ${file_src_list})
	get_filename_component(targetname ${file_src} NAME_WLA)
	add_executable(${targetname} ${file_src})
endforeach()
